import { LayoutHeaderDirective } from './layout-header.directive';

describe('LayoutHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
