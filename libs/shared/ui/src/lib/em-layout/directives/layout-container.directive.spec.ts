import { LayoutContainerDirective } from './layout-container.directive';

describe('LayoutContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutContainerDirective();
    expect(directive).toBeTruthy();
  });
});
