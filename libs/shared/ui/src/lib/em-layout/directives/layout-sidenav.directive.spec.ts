import { LayoutSidenavDirective } from './layout-sidenav.directive';

describe('LayoutSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
